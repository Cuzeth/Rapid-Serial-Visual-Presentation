# StrobeApp

- **Type:** Structure
- **Module:** Strobe
- **DocC Path:** `/documentation/strobe/strobeapp`

The app entry point. Bootstraps the SwiftData model container and displays a diagnostic error view if persistence initialization fails.

## API

### Initializers
- `init()`

### Instance Properties
- `var body: some Scene`
